class Solution {
    //ip는 4개의 byte로 이루어져있다. 각 바이트의 digit은 0 ~ 255 이다
    //digits들을 받아서 유효한 ip로 복원하면 된다
    //left = 0, right = 0 에서 시작한다
    //right는 움직이면서 digit에 값을 더한다
    //digit에 값을 더 한 값을 파라미터로 재귀를 돌린다
    //만약 digit이 byte보다 크담
    fun restoreIpAddresses(digits: String): ArrayList<String> {
        return arrayListOf()
    }
}
