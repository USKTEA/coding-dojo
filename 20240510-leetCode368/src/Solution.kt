class Solution {
    //[1, 2, 3] => [1, 2]
    //[1] => [1]
    //[1, 2, 4, 8] => [1, 2, 4, 8]
    //nums.sorted()
    //var left = 0
    //var right = 0
    //var bcf = 0
    //var result = set()
    //while (right < nums.size) {
    // val current = sorted[right]
    // if (bcf == 0) {
    // bcf = current
    // }
    // if (bcf % current == 0 || current % bcf == 0) {
    //
    // result.add(current)
    // }
    //
    // while (
    //
    //
    // right += 1
    // }
    //TODO 너 내가 푼다
    fun largestDivisibleSubset(nums: IntArray): List<Int> {
        return emptyList()
    }
}
